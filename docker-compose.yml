version: '3.8'

services:
  db:
    image: mysql:latest
    env_file:
      - .env.docker-compose
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
